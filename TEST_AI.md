# 🧪 Тестирование ИИ функционала

## ✅ Статус

**Сервер запущен и работает!**

- ✅ Backend сервер: `http://localhost:3000`
- ✅ Frontend сервер: `http://localhost:8000`
- ✅ OpenAI API ключ настроен
- ✅ API отвечает корректно

## 🚀 Как протестировать

### 1. Откройте виджет в браузере

```
http://localhost:8000/index.html
```

### 2. Протестируйте ИИ чат

1. Нажмите на виджет, чтобы открыть его
2. Нажмите кнопку **"Текст"**
3. Введите любой вопрос, например:
   - "Привет, расскажи о себе"
   - "Какие у вас услуги?"
   - "Сколько стоит?"

4. ИИ должен ответить на основе контекста сайта

### 3. Проверка через API напрямую

```bash
curl -X POST http://localhost:3000/chat \
  -H "Content-Type: application/json" \
  -d '{
    "siteDomain": "example.com",
    "userId": "test_user",
    "message": "Привет! Расскажи о ваших услугах"
  }'
```

## 📊 Ожидаемый результат

ИИ должен:
- ✅ Отвечать на вопросы пользователей
- ✅ Использовать контекст сайта (если сайт проиндексирован)
- ✅ Отвечать на русском языке
- ✅ Быть вежливым и профессиональным

## 🔍 Проверка логов

Логи сервера показывают:
- Запросы к OpenAI API
- Использованные токены
- Ошибки (если есть)

## 🐛 Troubleshooting

**ИИ не отвечает:**
- Проверьте консоль браузера (F12)
- Проверьте логи сервера
- Убедитесь, что сервер запущен на порту 3000

**Ошибка подключения:**
- Проверьте, что `WIDGET_API_URL` указан правильно
- Убедитесь, что нет проблем с CORS

**OpenAI ошибки:**
- Проверьте API ключ в `.env`
- Проверьте баланс OpenAI аккаунта
- Проверьте лимиты API

## 📝 Примеры тестовых запросов

```javascript
// Тест 1: Простое приветствие
{
  "siteDomain": "example.com",
  "userId": "user_123",
  "message": "Привет!"
}

// Тест 2: Вопрос об услугах
{
  "siteDomain": "example.com",
  "userId": "user_123",
  "message": "Какие услуги вы предоставляете?"
}

// Тест 3: Вопрос о ценах
{
  "siteDomain": "example.com",
  "userId": "user_123",
  "message": "Сколько стоят ваши услуги?"
}
```

## ✅ Готово к использованию!

ИИ функционал полностью настроен и готов к работе! 🎉

