# Интеграция Frontend и Backend

## 🔗 Подключение виджета к бэкенду

### Вариант 1: Виджет на том же домене

Если виджет размещен на том же домене, что и бэкенд, ничего дополнительно настраивать не нужно. API запросы будут идти относительно текущего домена.

### Вариант 2: Виджет на другом домене

Если виджет размещен на другом домене, нужно указать URL бэкенда:

```html
<script>
    // Укажите URL вашего бэкенда
    window.WIDGET_API_URL = 'https://your-backend-domain.com';
</script>
<script src="script.js"></script>
```

## 📋 Что изменилось во фронтенде

### 1. Автоматическое определение домена

Виджет автоматически определяет домен сайта, на котором он установлен:

```javascript
WidgetState.siteDomain = window.location.hostname.replace(/^www\./, '');
```

### 2. Интеграция с реальным API

Вместо имитации ответов, чат теперь отправляет запросы к `/chat`:

```javascript
ChatManager.sendMessage(messageText);
```

### 3. Сохранение истории

История диалога сохраняется в памяти виджета и отправляется с каждым запросом для контекста.

## 🔄 Процесс работы

1. **Пользователь отправляет сообщение**
   - Сообщение добавляется в UI
   - Показывается индикатор "печатает..."

2. **Запрос к бэкенду**
   - Отправляется POST `/chat` с:
     - `siteDomain` - домен сайта
     - `userId` - ID пользователя
     - `message` - текст сообщения
     - `history` - история диалога

3. **Обработка на бэкенде**
   - Проверка/запуск индексации сайта (если нужно)
   - Извлечение релевантного контекста из БД
   - Запрос к OpenAI API
   - Возврат ответа

4. **Отображение ответа**
   - Скрывается индикатор печати
   - Добавляется ответ ассистента в чат

## 🎯 FAQ интеграция

FAQ вопросы теперь также используют реальный API. При клике на вопрос:
- Открывается чат
- Вопрос отправляется в `/chat`
- Получается ответ на основе контекста сайта

## 🎤 Голосовые сообщения

Голосовые сообщения пока сохраняются через `/voice/save`, но в будущем можно добавить распознавание речи и отправку текста в `/chat`.

## ⚙️ Настройка

### Изменение URL API

Если нужно изменить базовый URL API:

```javascript
// В script.js или перед загрузкой виджета
window.WIDGET_API_URL = 'https://api.example.com';
```

### Отладка

В консоли браузера будут логи:
- Определенный домен сайта
- User ID
- URL API
- Метаданные ответов (токены, наличие контекста)

## 🚨 Обработка ошибок

При ошибках API:
- Показывается дружелюбное сообщение об ошибке
- История диалога сохраняется
- Пользователь может повторить запрос

## 📊 Мониторинг

Для мониторинга работы виджета можно отслеживать:
- Количество запросов к `/chat`
- Статус индексации через `/site/status`
- Логи на бэкенде

