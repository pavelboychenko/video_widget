# Video Widget - Интерактивный виджет для сбора вопросов

## 📋 Описание

Video Widget — это современный интерактивный виджет для веб-сайтов, предназначенный для сбора вопросов от пользователей через голосовые и текстовые сообщения. Виджет предоставляет удобный интерфейс для взаимодействия с клиентами и помогает собирать обратную связь, вопросы и запросы.

## 🎯 Назначение

Виджет разработан для:
- **Сбора вопросов от пользователей** через голосовые и текстовые сообщения
- **Предоставления быстрых ответов** на часто задаваемые вопросы (FAQ)
- **Улучшения пользовательского опыта** с помощью интуитивного интерфейса
- **Интеграции с CRM/системами** для обработки запросов клиентов
- **Увеличения конверсии** за счет удобного способа связи

## ✨ Основные возможности

### 1. Видео превью
- Привлекательное видео превью в свернутом состоянии
- Плавная анимация при наведении
- Автоматическое воспроизведение при открытии

### 2. Голосовые сообщения
- Запись голосовых вопросов через микрофон браузера
- Визуализация звука в реальном времени
- Таймер записи с ограничением времени (до 5 минут)
- Автоматическая обработка и отправка на сервер

### 3. Текстовые сообщения
- Ввод текстовых вопросов
- Быстрый доступ к FAQ
- Счетчик символов с валидацией
- Поддержка горячих клавиш (Ctrl+Enter)

### 4. Чат с ассистентом
- Интерактивный чат-интерфейс
- История сообщений
- Индикатор печати ассистента
- Статус онлайн/оффлайн

### 5. FAQ (Часто задаваемые вопросы)
- Быстрый доступ к популярным вопросам
- Автоматические ответы на предустановленные вопросы
- Удобная навигация

## 📁 Структура проекта

```
IP/
├── index.html          # HTML структура виджета
├── styles.css          # Стили и анимации
├── script.js           # JavaScript логика
└── README.md          # Документация
```

## 🚀 Быстрый старт

### 1. Установка

Просто подключите файлы к вашему HTML странице:

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ваш сайт</title>
    <link rel="stylesheet" href="styles.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
</head>
<body>
    <!-- Ваш контент -->
    
    <!-- Виджет -->
    <script src="script.js"></script>
    <!-- Скопируйте HTML из index.html -->
</body>
</html>
```

### 2. Настройка видео

Поместите видео файл в корень проекта или укажите правильный путь:

```html
<video id="video-widget__video" ...>
    <source src="/путь/к/вашему/видео.mp4" type="video/mp4">
</video>
```

### 3. Настройка backend API

Виджет отправляет данные на следующие endpoints:

#### Голосовые сообщения
```
POST /voice/save
Content-Type: multipart/form-data

Body:
- audio: Blob (audio/webm)
- userId: string
- duration: number (секунды)
- timestamp: string (ISO 8601)
```

#### Текстовые сообщения
```
POST /text/save
Content-Type: application/json

Body:
{
    "userId": "string",
    "text": "string",
    "timestamp": "string (ISO 8601)"
}
```

## ⚙️ Конфигурация

В файле `script.js` можно настроить параметры:

```javascript
const CONFIG = {
    maxRecordingTime: 300,    // Максимальное время записи (секунды)
    maxTextLength: 500,       // Максимальная длина текстового вопроса
    maxChatLength: 1000,      // Максимальная длина сообщения в чате
    typingDelay: 1000,        // Задержка перед показом индикатора печати
    answerDelay: 1500,        // Задержка перед ответом ассистента
};
```

## 🎨 Кастомизация

### Цветовая схема

В файле `styles.css` определены CSS переменные:

```css
:root {
    --primary-color: #FF4F00;      /* Основной цвет */
    --primary-dark: #E04500;      /* Темный оттенок */
    --primary-light: #FF7E00;     /* Светлый оттенок */
    --secondary-color: #131344;   /* Вторичный цвет */
    --bg-color: #ffffff;          /* Фон */
    /* ... и другие */
}
```

### Изменение FAQ

Отредактируйте HTML в `index.html`:

```html
<div class="faq-message" data-question="Ваш вопрос">
    <span class="faq-icon">💡</span>
    <span class="faq-text">Ваш вопрос</span>
</div>
```

И добавьте ответ в `script.js`:

```javascript
const FAQManager = {
    answers: {
        "Ваш вопрос": "Ваш ответ здесь",
        // ...
    }
}
```

## 📱 Адаптивность

Виджет полностью адаптивен и корректно работает на:
- Десктопах (1920px+)
- Планшетах (768px - 1919px)
- Мобильных устройствах (< 768px)

## 🔧 API и интеграция

### События виджета

Виджет можно расширить, добавив обработчики событий:

```javascript
// Пример: отслеживание открытия виджета
$(document).on('widget:opened', function() {
    console.log('Виджет открыт');
});

// Пример: отслеживание отправки сообщения
$(document).on('widget:message:sent', function(e, data) {
    console.log('Сообщение отправлено:', data);
});
```

### Получение User ID

Каждый пользователь получает уникальный ID:

```javascript
const userId = WidgetState.userId;
// Пример: 'user_abc123xyz_1234567890'
```

## 🎯 Сценарии использования

### 1. Сбор вопросов от клиентов
Виджет позволяет клиентам быстро задать вопрос, не покидая страницу.

### 2. Предпродажная поддержка
FAQ помогает ответить на типичные вопросы до обращения к менеджеру.

### 3. Сбор обратной связи
Голосовые сообщения удобны для получения развернутой обратной связи.

### 4. Интеграция с CRM
Все запросы отправляются на ваш сервер для дальнейшей обработки.

## 🔒 Безопасность

- Все данные отправляются через HTTPS
- User ID генерируется на клиенте
- Нет хранения персональных данных в браузере
- Запрос доступа к микрофону требует явного разрешения пользователя

## 🌐 Браузерная поддержка

- Chrome/Edge 90+
- Firefox 88+
- Safari 14+
- Opera 76+

**Требования:**
- JavaScript включен
- Поддержка MediaRecorder API (для голосовых сообщений)
- Поддержка Fetch API

## 📊 Аналитика

Для отслеживания использования виджета можно добавить:

```javascript
// Отслеживание открытий
$('.video-widget__container').on('click', function() {
    // Ваш код аналитики
    gtag('event', 'widget_opened');
});

// Отслеживание отправки сообщений
// В функции sendAudioToServer или sendTextQuestion
gtag('event', 'message_sent', {
    'message_type': 'voice' // или 'text'
});
```

## 🐛 Решение проблем

### Видео не воспроизводится
- Проверьте путь к видео файлу
- Убедитесь, что формат поддерживается браузером (MP4)
- Проверьте права доступа к файлу

### Микрофон не работает
- Убедитесь, что пользователь разрешил доступ к микрофону
- Проверьте, что сайт открыт по HTTPS (требование браузера)
- Проверьте настройки браузера

### Сообщения не отправляются
- Проверьте, что backend endpoints доступны
- Проверьте CORS настройки на сервере
- Откройте консоль браузера для просмотра ошибок

## 📝 Лицензия

Этот виджет предоставляется "как есть" для использования в ваших проектах.

## 🤝 Поддержка

Для вопросов и предложений создайте issue в репозитории проекта.

## 📈 Планы развития

- [ ] Поддержка нескольких языков
- [ ] Интеграция с популярными CRM системами
- [ ] Расширенная аналитика
- [ ] Темная тема
- [ ] Дополнительные типы медиа (фото, файлы)

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024

# video_widget
