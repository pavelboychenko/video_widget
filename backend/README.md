# Video Widget Backend

Backend сервис для Video Widget с интеграцией OpenAI API и автоматическим парсингом сайтов.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd backend
npm install
```

### 2. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните:

```bash
cp .env.example .env
```

Обязательно укажите:
- `OPENAI_API_KEY` - ваш API ключ OpenAI
- `OPENAI_MODEL` - модель (рекомендуется `gpt-4o-mini`)

### 3. Запуск сервера

```bash
npm start
```

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

Сервер запустится на порту 3000 (или указанном в `.env`).

## 📁 Структура проекта

```
backend/
├── config.js              # Конфигурация приложения
├── server.js              # Главный файл сервера
├── database/
│   └── db.js              # Инициализация БД (SQLite)
├── services/
│   ├── SiteCrawler.js     # Парсинг и индексация сайтов
│   ├── SiteKnowledgeBase.js # Извлечение контекста из БД
│   └── ChatService.js     # Работа с OpenAI API
├── routes/
│   ├── chat.js            # API endpoints для чата
│   └── legacy.js          # Legacy endpoints (/voice/save, /text/save)
└── data/                  # База данных SQLite (создается автоматически)
```

## 🔌 API Endpoints

### POST /chat

Основной endpoint для чата с ИИ.

**Request:**
```json
{
  "siteDomain": "example.com",
  "userId": "user_123_456",
  "message": "Сколько стоит ваша услуга?",
  "history": [
    { "role": "user", "content": "Привет" },
    { "role": "assistant", "content": "Здравствуйте!" }
  ]
}
```

**Response:**
```json
{
  "success": true,
  "answer": "Наша услуга стоит от 10 000 рублей...",
  "meta": {
    "usedSiteDomain": "example.com",
    "hasContext": true,
    "tokens": {
      "prompt": 500,
      "completion": 100,
      "total": 600
    }
  }
}
```

### POST /site/init

Ручной запуск индексации сайта.

**Request:**
```json
{
  "siteDomain": "example.com"
}
```

**Response:**
```json
{
  "success": true,
  "status": "started",
  "message": "Indexing started"
}
```

### GET /site/status

Получить статус индексации сайта.

**Query:**
```
?siteDomain=example.com
```

**Response:**
```json
{
  "success": true,
  "status": "completed",
  "pagesCount": 45,
  "hasIndexedPages": true,
  "lastIndexedAt": "2024-01-15T10:30:00.000Z"
}
```

### POST /voice/save (Legacy)

Сохранение голосовых сообщений.

### POST /text/save (Legacy)

Сохранение текстовых сообщений.

## 🔧 Конфигурация

Все настройки в файле `.env`:

- `OPENAI_API_KEY` - API ключ OpenAI
- `OPENAI_MODEL` - модель GPT (gpt-4o-mini, gpt-4, etc.)
- `PORT` - порт сервера (по умолчанию 3000)
- `CRAWLER_MAX_DEPTH` - максимальная глубина обхода (по умолчанию 3)
- `CRAWLER_MAX_PAGES` - максимальное количество страниц (по умолчанию 100)
- `MAX_CONTEXT_LENGTH` - максимальная длина контекста (по умолчанию 8000)

## 🗄️ База данных

Используется SQLite. База создается автоматически при первом запуске.

**Таблицы:**
- `site_pages` - проиндексированные страницы сайтов
- `site_indexing_status` - статус индексации по доменам
- `chat_sessions` - сессии чата (для аналитики)

## 🔄 Как работает индексация

1. При первом запросе от домена автоматически запускается индексация (если включено)
2. Crawler обходит сайт, начиная с главной страницы
3. Для каждой страницы извлекается текст, определяется тип (services, pricing, faq, etc.)
4. Данные сохраняются в БД
5. При запросах к `/chat` используется контекст из проиндексированных страниц

## 🛡️ Безопасность

- Каждый домен изолирован (данные не смешиваются)
- Валидация всех входных данных
- Обработка ошибок OpenAI API
- Логирование для отладки

## 📝 Логирование

Все важные события логируются в консоль:
- Запуск/завершение индексации
- Ошибки OpenAI API
- Ошибки парсинга

## 🧪 Тестирование

Для тестирования можно использовать curl:

```bash
# Тест чата
curl -X POST http://localhost:3000/chat \
  -H "Content-Type: application/json" \
  -d '{
    "siteDomain": "example.com",
    "userId": "test_user",
    "message": "Привет"
  }'

# Запуск индексации
curl -X POST http://localhost:3000/site/init \
  -H "Content-Type: application/json" \
  -d '{"siteDomain": "example.com"}'
```

## 🚨 Troubleshooting

**Ошибка "OpenAI API key is not configured"**
- Проверьте, что `.env` файл существует и содержит `OPENAI_API_KEY`

**Индексация не запускается**
- Проверьте логи в консоли
- Убедитесь, что сайт доступен по HTTPS
- Проверьте настройки `CRAWLER_MAX_DEPTH` и `CRAWLER_MAX_PAGES`

**Чат не отвечает**
- Проверьте, что индексация завершена (`GET /site/status`)
- Проверьте логи ошибок OpenAI API
- Убедитесь, что есть проиндексированные страницы

