# Настройка переменных окружения

## ✅ API ключ OpenAI настроен

Файл `.env` создан с вашим API ключом OpenAI.

## 📋 Содержимое .env файла

```
OPENAI_API_KEY=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
OPENAI_MODEL=gpt-4o-mini
PORT=3000
NODE_ENV=development
```

## 🚀 Готово к запуску

Теперь можно запустить сервер:

```bash
cd backend
npm install  # если еще не установлены зависимости
npm start
```

Сервер автоматически загрузит API ключ из файла `.env`.

## 🔒 Безопасность

⚠️ **Важно**: Файл `.env` находится в `.gitignore` и не будет загружен в репозиторий. Это правильно для безопасности.

## 📝 Дополнительные настройки

Если нужно изменить настройки, отредактируйте файл `backend/.env`:

- `OPENAI_MODEL` - модель GPT (gpt-4o-mini, gpt-4, gpt-4-turbo)
- `PORT` - порт сервера (по умолчанию 3000)
- `CRAWLER_MAX_DEPTH` - глубина обхода сайта
- `CRAWLER_MAX_PAGES` - максимальное количество страниц

