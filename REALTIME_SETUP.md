# 🚀 Настройка Real-Time коммуникаций

## Обзор

Добавлен функционал живого общения с менеджерами через WebSocket и WebRTC для аудио/видео звонков.

## 📦 Установка зависимостей

```bash
cd backend
npm install
```

Новые зависимости:
- `socket.io` - WebSocket сервер
- `bcrypt` - хеширование паролей
- `jsonwebtoken` - JWT токены (опционально)

## 🔧 Настройка

### Backend

1. Убедитесь, что `.env` файл настроен
2. Запустите сервер:
```bash
npm start
```

Сервер автоматически создаст:
- WebSocket сервер на порту 3000
- Namespace `/chat` для текстового чата
- Namespace `/call` для WebRTC signaling

### Frontend (Виджет)

Виджет автоматически определяет WebSocket URL. Если нужно указать вручную:

```html
<script>
    window.WIDGET_WS_URL = 'ws://localhost:3000'; // или wss:// для HTTPS
</script>
```

### Админ-панель

Откройте `admin/index.html` в браузере или разместите на сервере.

Для настройки URL API:
```html
<script>
    window.ADMIN_API_URL = 'http://localhost:3000';
    window.ADMIN_WS_URL = 'ws://localhost:3000';
</script>
```

## 👥 Создание менеджера

### Через API

```bash
curl -X POST http://localhost:3000/manager/create \
  -H "Content-Type: application/json" \
  -d '{
    "email": "manager@example.com",
    "password": "secure_password",
    "name": "Иван Иванов",
    "siteDomain": "example.com"
  }'
```

### Через SQL (для тестирования)

```sql
INSERT INTO managers (id, email, password_hash, name, site_domain, online)
VALUES (
    'mngr_test_123',
    'test@example.com',
    '$2b$10$...', -- bcrypt hash пароля
    'Тестовый менеджер',
    'example.com',
    0
);
```

## 🔄 Процесс работы

### 1. Пользователь запрашивает менеджера

1. Нажимает "С менеджером" в виджете
2. Виджет отправляет `POST /manager/request`
3. Создается сессия со статусом `waiting`
4. Сессия добавляется в очередь
5. Все онлайн менеджеры получают уведомление через WebSocket

### 2. Менеджер принимает запрос

1. Менеджер видит запрос в очереди (админ-панель)
2. Нажимает на запрос
3. Отправляется событие `manager_accept` через WebSocket
4. Сессия переходит в статус `active`
5. Пользователь получает событие `manager_connected`
6. Чат переключается в live-режим

### 3. Обмен сообщениями

- Сообщения отправляются через WebSocket событие `chat_message`
- Сервер ретранслирует сообщения между пользователем и менеджером
- Все сообщения сохраняются в БД

### 4. Звонок (WebRTC)

1. Пользователь или менеджер нажимает кнопку звонка
2. Создается RTCPeerConnection
3. Генерируется offer и отправляется через WebSocket (`/call` namespace)
4. Вторая сторона получает offer, создает answer
5. Обмен ICE candidates
6. Устанавливается peer-to-peer соединение
7. Аудио/видео потоки передаются напрямую

## 🎯 API Endpoints

### POST /manager/request
Запрос менеджера от пользователя

### POST /manager/auth
Авторизация менеджера

### GET /manager/queue
Получить очередь запросов

### GET /manager/sessions
Получить активные сессии менеджера

### POST /manager/create
Создание нового менеджера

## 🔌 WebSocket Events

### Namespace: /chat

**От пользователя:**
- `user_connect` - подключение пользователя
- `chat_message` - отправка сообщения

**От менеджера:**
- `manager_connect` - подключение менеджера
- `manager_accept` - принятие сессии
- `chat_message` - отправка сообщения

**От сервера:**
- `manager_connected` - менеджер подключился
- `chat_message` - новое сообщение
- `chat_history` - история сообщений
- `new_session_available` - новый запрос в очереди
- `queue_update` - обновление очереди

### Namespace: /call

**WebRTC signaling:**
- `join_session` - присоединение к сессии звонка
- `call_offer` - WebRTC offer
- `call_answer` - WebRTC answer
- `ice_candidate` - ICE candidate
- `call_end` - завершение звонка

## 🗄️ База данных

### Новые таблицы:

**managers**
- Хранение менеджеров
- Пароли хешируются через bcrypt

**chat_sessions** (обновлена)
- Добавлены поля: `session_id`, `manager_id`, `status`
- Статусы: `waiting`, `active`, `closed`

**chat_messages** (новая)
- История всех сообщений
- Привязка к сессии

## 🎨 UI Состояния

### Виджет:
- `waiting_for_manager` - ожидание подключения
- `manager_connected` - менеджер подключен
- `call_in_progress` - идет звонок

### Админ-панель:
- Экран авторизации
- Основной интерфейс с очередью
- Активная сессия с чатом
- Модальное окно видеозвонка

## 🔒 Безопасность

- Пароли менеджеров хешируются (bcrypt)
- Каждая сессия изолирована по домену
- WebSocket соединения проверяют авторизацию
- WebRTC использует STUN серверы (можно добавить TURN)

## 🧪 Тестирование

### 1. Создайте менеджера
```bash
curl -X POST http://localhost:3000/manager/create \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"test123","name":"Test Manager"}'
```

### 2. Откройте админ-панель
- Откройте `admin/index.html`
- Войдите с созданными credentials

### 3. Откройте виджет
- Откройте страницу с виджетом
- Нажмите "С менеджером"
- В админ-панели появится запрос в очереди
- Примите запрос
- Начните общение!

## 📝 Примечания

- WebRTC требует HTTPS в продакшене (или localhost для разработки)
- Для работы через NAT может потребоваться TURN сервер
- STUN сервер по умолчанию: `stun.l.google.com:19302`

## 🚨 Troubleshooting

**WebSocket не подключается:**
- Проверьте, что сервер запущен
- Проверьте CORS настройки
- Проверьте URL в консоли браузера

**Менеджер не видит запросы:**
- Убедитесь, что менеджер онлайн
- Проверьте WebSocket соединение
- Проверьте логи сервера

**WebRTC не работает:**
- Проверьте разрешения на камеру/микрофон
- Убедитесь, что используется HTTPS (или localhost)
- Проверьте консоль браузера на ошибки

