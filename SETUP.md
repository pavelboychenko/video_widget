# 🚀 Инструкция по запуску Video Widget с OpenAI

## Шаг 1: Настройка бэкенда

### 1.1. Установка зависимостей

```bash
cd backend
npm install
```

### 1.2. Настройка окружения

Создайте файл `.env` в папке `backend/`:

```bash
cd backend
cp .env.example .env
```

Отредактируйте `.env` и укажите:

```env
OPENAI_API_KEY=sk-your-actual-api-key-here
OPENAI_MODEL=gpt-4o-mini
PORT=3000
```

### 1.3. Запуск бэкенда

```bash
npm start
```

Сервер запустится на `http://localhost:3000`

Для разработки с автоперезагрузкой:
```bash
npm run dev
```

## Шаг 2: Настройка фронтенда

### 2.1. Указание URL бэкенда (если нужно)

Если виджет размещен на другом домене, чем бэкенд, укажите URL в HTML:

```html
<script>
    window.WIDGET_API_URL = 'http://localhost:3000'; // или ваш production URL
</script>
<script src="script.js"></script>
```

Если виджет на том же домене - ничего менять не нужно.

### 2.2. Запуск локального сервера для тестирования

```bash
# В корне проекта
python3 -m http.server 8000
```

Откройте `http://localhost:8000/index.html`

## Шаг 3: Первый запуск

### 3.1. Проверка работы

1. Откройте виджет на странице
2. Откройте чат
3. Отправьте любое сообщение

**Что произойдет:**
- Виджет автоматически определит домен сайта
- При первом запросе запустится индексация сайта (в фоне)
- Пока индексация идет, ИИ будет отвечать на основе уже собранных данных (если есть)
- После завершения индексации ответы станут более точными

### 3.2. Проверка статуса индексации

```bash
curl "http://localhost:3000/site/status?siteDomain=localhost"
```

Или в браузере:
```
http://localhost:3000/site/status?siteDomain=localhost
```

## Шаг 4: Ручной запуск индексации (опционально)

Если хотите запустить индексацию вручную:

```bash
curl -X POST http://localhost:3000/site/init \
  -H "Content-Type: application/json" \
  -d '{"siteDomain": "example.com"}'
```

## 📋 Структура проекта после настройки

```
IP/
├── index.html              # Фронтенд виджета
├── script.js               # JavaScript виджета (обновлен)
├── styles.css              # Стили
├── sphere.svg              # Анимация сферы
├── backend/                # Бэкенд сервис
│   ├── server.js
│   ├── config.js
│   ├── package.json
│   ├── .env                # ⚠️ Создайте этот файл!
│   ├── database/
│   │   └── db.js
│   ├── services/
│   │   ├── SiteCrawler.js
│   │   ├── SiteKnowledgeBase.js
│   │   └── ChatService.js
│   └── routes/
│       ├── chat.js
│       └── legacy.js
└── data/                   # База данных (создается автоматически)
    └── widget.db
```

## 🔍 Проверка работы

### Тест API напрямую

```bash
curl -X POST http://localhost:3000/chat \
  -H "Content-Type: application/json" \
  -d '{
    "siteDomain": "example.com",
    "userId": "test_user_123",
    "message": "Привет, расскажи о ваших услугах"
  }'
```

### Проверка логов

В консоли бэкенда вы увидите:
- ✅ Запуск сервера
- 📄 Процесс индексации страниц
- 🤖 Запросы к OpenAI
- ⚠️ Ошибки (если есть)

## 🚨 Решение проблем

### Ошибка "OpenAI API key is not configured"
- Проверьте файл `backend/.env`
- Убедитесь, что ключ указан правильно

### Индексация не запускается
- Проверьте, что сайт доступен по HTTPS
- Проверьте логи в консоли бэкенда
- Убедитесь, что `AUTO_INDEX_ON_FIRST_REQUEST=true` в `.env`

### Чат не отвечает
- Проверьте статус индексации: `GET /site/status`
- Проверьте логи ошибок OpenAI
- Убедитесь, что есть проиндексированные страницы

### CORS ошибки
- Убедитесь, что бэкенд разрешает запросы с вашего домена
- Проверьте настройки CORS в `server.js`

## 📊 Мониторинг

### Просмотр проиндексированных страниц

База данных SQLite находится в `backend/data/widget.db`

Можно использовать SQLite CLI:
```bash
sqlite3 backend/data/widget.db
SELECT site_domain, url, type FROM site_pages LIMIT 10;
```

### Логи

Все важные события логируются в консоль:
- Индексация страниц
- Запросы к OpenAI
- Ошибки

## 🎯 Следующие шаги

1. ✅ Настроить production окружение
2. ✅ Настроить CORS для конкретных доменов
3. ✅ Добавить rate limiting
4. ✅ Настроить мониторинг и алерты
5. ✅ Оптимизировать парсинг для больших сайтов

## 📝 Важные замечания

- **Безопасность**: В продакшене ограничьте CORS конкретными доменами
- **Производительность**: Для больших сайтов увеличьте `CRAWLER_MAX_PAGES`
- **Стоимость**: Следите за использованием токенов OpenAI API
- **База данных**: SQLite подходит для начала, для продакшена рассмотрите PostgreSQL

